#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include <string.h>

//struct Book
//{
//	char name[20];//字符数组
//	short price;//整数定价
//};//分号不可缺少，用于结束struct
//
//int main()//利用结构体类型创建该类型的结构体变量
//{
//	struct Book b1 = { "设计（书名）",55 };//55为定价
//	struct Book* pb = &b1;//指针
//	printf("%s\n", pb->name);//方法2
//	//利用pb打印书名和价格
//	//printf("%s\n", (*pb).name);//方法1
//	//printf("%d\n", (*pb).price);
////	printf("书名：%s\n", b1.name);//.name用于找到结构体中的元素
////	printf("价格：%d\n", b1.price);
////	b1.price = 15;
////	printf("修改后的价格：%d\n", b1.price);//修改价格
//	strcpy(b1.name,"设计2（更改的书名）");//拷贝字符串并替换
//	printf("书名：%s\n", b1.name);
//	return 0;
//}


//int main()
//{
//	int input = 0;
//	printf("start\n");
//	printf("test question?(0/1)>:");
//	scanf("%d", &input);   //赋值
//		if (input == 1)   //比较两者大小
//			printf("good");
//		else
//			printf("bad");
//	return 0;
//}



